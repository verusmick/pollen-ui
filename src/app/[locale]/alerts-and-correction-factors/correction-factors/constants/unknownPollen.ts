export const UNKNOWN_POLLEN_CODE = 'UNKNOWN' as const;

// TODO: Replace this unresolved placeholder once backend confirms how Unknown is encoded.
// This constant exists to keep that backend dependency isolated to one mapping boundary.
export const UNKNOWN_POLLEN_API_VALUE: string | null | undefined = undefined;
